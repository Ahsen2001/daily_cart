<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="text-xl font-semibold">{{ __('Vendor Coupons') }}</h2><a href="{{ route('vendor.coupons.create') }}" class="text-sm text-indigo-700 underline">{{ __('Create') }}</a></div></x-slot>
<div class="py-12"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="bg-white p-6 shadow-sm sm:rounded-lg"><table class="min-w-full divide-y divide-gray-200"><tbody class="divide-y divide-gray-100 text-sm">@forelse($coupons as $coupon)<tr><td class="px-3 py-3 font-medium">{{ $coupon->code }}</td><td class="px-3 py-3">{{ $coupon->title }}</td><td class="px-3 py-3">{{ ucfirst($coupon->status) }}</td><td class="px-3 py-3">{{ $coupon->used_count }}/{{ $coupon->usage_limit ?? '-' }}</td><td class="px-3 py-3 text-right"><a href="{{ route('vendor.coupons.edit', $coupon) }}" class="text-indigo-700 underline">{{ __('Edit') }}</a></td></tr>@empty<tr><td class="px-3 py-6 text-center text-gray-500">{{ __('No coupons found.') }}</td></tr>@endforelse</tbody></table><div class="mt-6">{{ $coupons->links() }}</div></div></div></div></x-app-layout>
