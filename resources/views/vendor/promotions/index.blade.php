<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="text-xl font-semibold">{{ __('Vendor Promotions') }}</h2><a href="{{ route('vendor.promotions.create') }}" class="text-sm text-indigo-700 underline">{{ __('Create') }}</a></div></x-slot><div class="py-12"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="bg-white p-6 shadow-sm sm:rounded-lg"><table class="min-w-full divide-y divide-gray-200"><tbody class="divide-y divide-gray-100 text-sm">@forelse($promotions as $promotion)<tr><td class="px-3 py-3 font-medium">{{ $promotion->title }}</td><td class="px-3 py-3">{{ str_replace('_',' ',ucfirst($promotion->promotion_type)) }}</td><td class="px-3 py-3">{{ ucfirst($promotion->status) }}</td><td class="px-3 py-3">{{ $promotion->views_count }} views</td><td class="px-3 py-3 text-right"><a href="{{ route('vendor.promotions.edit', $promotion) }}" class="text-indigo-700 underline">{{ __('Edit') }}</a></td></tr>@empty<tr><td class="px-3 py-6 text-center text-gray-500">{{ __('No promotions found.') }}</td></tr>@endforelse</tbody></table><div class="mt-6">{{ $promotions->links() }}</div></div></div></div></x-app-layout>
