<x-app-layout><x-slot name="header"><div><p class="dc-page-eyebrow">{{ __('My Store') }}</p><h2 class="dc-page-title">{{ __('Store Banners') }}</h2></div></x-slot><div class="dc-page-section"><div class="dc-container grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><form method="POST" action="{{ route('vendor.store.banners.store') }}" enctype="multipart/form-data" class="dc-panel space-y-4">@csrf <h3 class="text-lg font-extrabold">{{ __('Upload banner') }}</h3><div><x-input-label for="title" :value="__('Title')"/><x-text-input id="title" name="title" class="mt-1 block w-full"/></div><div><x-input-label for="link_url" :value="__('Optional link')"/><x-text-input id="link_url" name="link_url" type="url" class="mt-1 block w-full"/></div><div><x-input-label for="image" :value="__('Banner image')"/><input id="image" name="image" type="file" required accept="image/*" class="mt-2 block w-full text-sm"/></div><button class="dc-button" type="submit">{{ __('Upload Banner') }}</button></form><section class="space-y-4">@if(session('status'))<div class="rounded-2xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">{{ session('status') }}</div>@endif @forelse($profile->banners as $banner)<article class="dc-card flex flex-col gap-4 sm:flex-row"><img src="{{ $banner->image_url }}" alt="" class="h-28 w-full rounded-2xl object-cover sm:w-56"><div class="flex-1"><h3 class="font-extrabold">{{ $banner->title ?: __('Store banner') }}</h3><p class="mt-1 text-sm text-brand-text/60">{{ $banner->link_url ?: __('No link') }}</p><form class="mt-4" method="POST" action="{{ route('vendor.store.banners.destroy', $banner) }}">@csrf @method('DELETE')<button class="text-sm font-bold text-red-700">{{ __('Remove') }}</button></form></div></article>@empty<p class="dc-card text-sm text-brand-text/65">{{ __('No store banners yet.') }}</p>@endforelse</section></div></div></x-app-layout>
