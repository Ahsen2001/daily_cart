<x-app-layout><x-slot name="header"><div><p class="dc-page-eyebrow">{{ __('My Store') }}</p><h2 class="dc-page-title">{{ __('Store Analytics') }}</h2></div></x-slot><div class="dc-page-section"><div class="dc-container"><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">@foreach($metrics as $label => $value)<div class="dc-card"><p class="dc-page-eyebrow">{{ __(str_replace('_', ' ', $label)) }}</p><p class="mt-2 text-2xl font-extrabold">{{ $label === 'revenue' ? \App\Services\CurrencyService::formatLkr($value) : ($label === 'average_rating' ? ($value ?: '-') : number_format($value)) }}</p></div>@endforeach</div><a class="dc-button-secondary mt-6" href="{{ route('vendor.store.edit') }}">{{ __('Store Settings') }}</a></div></div></x-app-layout>
