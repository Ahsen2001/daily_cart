<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">{{ __('Edit Coupon') }}</h2></x-slot><div class="py-12"><div class="mx-auto max-w-4xl sm:px-6 lg:px-8"><div class="bg-white p-6 shadow-sm sm:rounded-lg">@if(session('status'))<div class="mb-4 text-sm text-green-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('admin.coupons.update', $coupon) }}">@include('vendor.coupons._form', ['method' => 'PATCH', 'vendors' => $vendors])</form></div></div></div></x-app-layout>
