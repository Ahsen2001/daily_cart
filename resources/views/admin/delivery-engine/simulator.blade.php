<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">{{ __('Delivery Simulator') }}</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-3xl space-y-6 sm:px-6 lg:px-8">
        <form method="GET" class="grid gap-3 rounded-lg bg-white p-6 shadow sm:grid-cols-2"><x-text-input name="subtotal" type="number" min="0" step="0.01" :value="request('subtotal')" placeholder="Cart subtotal" required /><x-text-input name="district" :value="request('district')" placeholder="District" /><x-text-input name="province" :value="request('province')" placeholder="Province" /><x-text-input name="distance_meters" type="number" min="0" :value="request('distance_meters')" placeholder="Distance in meters" /><x-primary-button>{{ __('Simulate') }}</x-primary-button></form>
        @if($estimate)<div class="rounded-lg bg-white p-6 shadow"><h3 class="text-lg font-bold">{{ __('Quote result') }}</h3><dl class="mt-4 grid gap-3 sm:grid-cols-2"><div><dt class="text-sm text-gray-500">Delivery fee</dt><dd class="text-xl font-bold">Rs. {{ number_format($estimate['delivery_fee'],2) }}</dd></div><div><dt class="text-sm text-gray-500">Estimated time</dt><dd class="text-xl font-bold">{{ $estimate['estimated_delivery_minutes'] ? $estimate['estimated_delivery_minutes'].' min' : 'Not configured' }}</dd></div><div><dt class="text-sm text-gray-500">Matched rule</dt><dd>{{ ucfirst(str_replace('_',' ',$estimate['rule_scope'])) }}</dd></div><div><dt class="text-sm text-gray-500">Free delivery</dt><dd>{{ $estimate['free_delivery_eligible'] ? 'Eligible' : 'Not eligible' }}</dd></div></dl></div>@endif
    </div></div>
</x-app-layout>
