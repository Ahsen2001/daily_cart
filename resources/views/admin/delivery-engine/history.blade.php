<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">{{ __('Delivery Rule History') }}</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-6xl sm:px-6 lg:px-8"><div class="overflow-x-auto rounded-lg bg-white shadow"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-left"><tr><th class="p-3">When</th><th>Rule</th><th>Action</th><th>By</th><th>Changed fields</th></tr></thead><tbody class="divide-y">@forelse($histories as $history)<tr><td class="p-3">{{ $history->created_at->format('Y-m-d H:i') }}</td><td>{{ $history->rule?->zone?->name ?? $history->rule?->district ?? $history->rule?->scope ?? 'Deleted rule' }}</td><td>{{ ucfirst($history->action) }}</td><td>{{ $history->user?->name ?? 'System' }}</td><td>{{ implode(', ', array_keys($history->changes ?? [])) }}</td></tr>@empty<tr><td colspan="5" class="p-6 text-center text-gray-500">No rule changes recorded.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $histories->links() }}</div></div></div></div>
</x-app-layout>
