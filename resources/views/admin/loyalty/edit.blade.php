<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">{{ __('Loyalty Settings') }}</h2></x-slot><div class="py-12"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="mb-6 bg-white p-6 shadow-sm sm:rounded-lg">@if(session('status'))<div class="mb-4 text-sm text-green-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('admin.loyalty-settings.update') }}" class="grid gap-4 md:grid-cols-3">@csrf @method('PATCH')<div><x-input-label for="spend_amount_per_point" :value="__('Spend Amount Per Point')" /><x-text-input id="spend_amount_per_point" name="spend_amount_per_point" type="number" class="mt-1 w-full" :value="$setting->spend_amount_per_point" required /></div><div><x-input-label for="redemption_value_per_point" :value="__('Redemption Value Per Point')" /><x-text-input id="redemption_value_per_point" name="redemption_value_per_point" type="number" step="0.01" class="mt-1 w-full" :value="$setting->redemption_value_per_point" required /></div><div><x-input-label for="status" :value="__('Status')" /><select id="status" name="status" class="mt-1 w-full rounded-md border-gray-300 shadow-sm"><option value="active" @selected($setting->status === 'active')>{{ __('Active') }}</option><option value="inactive" @selected($setting->status === 'inactive')>{{ __('Inactive') }}</option></select></div><div class="md:col-span-3"><x-primary-button>{{ __('Save Settings') }}</x-primary-button></div></form></div><div class="bg-white p-6 shadow-sm sm:rounded-lg"><h3 class="mb-4 font-semibold">{{ __('Customer Loyalty Reports') }}</h3><table class="min-w-full divide-y divide-gray-200"><tbody class="divide-y divide-gray-100 text-sm">@foreach($customers as $customer)<tr><td class="px-3 py-3">{{ $customer->user?->name }}</td><td class="px-3 py-3">{{ $customer->loyaltyBalance() }} {{ __('points') }}</td></tr>@endforeach</tbody></table><div class="mt-6">{{ $customers->links() }}</div></div></div></div></x-app-layout>
