<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="text-xl font-semibold">{{ __('Advertisement Management') }}</h2><a href="{{ route('admin.advertisements.create') }}" class="text-sm text-indigo-700 underline">{{ __('Create') }}</a></div></x-slot><div class="py-12"><div class="mx-auto max-w-7xl sm:px-6 lg:px-8"><div class="bg-white p-6 shadow-sm sm:rounded-lg"><table class="min-w-full divide-y divide-gray-200"><tbody class="divide-y divide-gray-100 text-sm">@forelse($advertisements as $ad)<tr><td class="px-3 py-3 font-medium">{{ $ad->title }}</td><td class="px-3 py-3">{{ $ad->vendor?->store_name ?? '-' }}</td><td class="px-3 py-3">{{ str_replace('_',' ',ucfirst($ad->position ?? $ad->placement)) }}</td><td class="px-3 py-3">{{ ucfirst($ad->status) }}</td><td class="px-3 py-3">{{ $ad->views_count }} views / {{ $ad->clicks_count }} clicks</td><td class="px-3 py-3 text-right"><a href="{{ route('admin.advertisements.edit', $ad) }}" class="text-indigo-700 underline">{{ __('Edit') }}</a></td></tr>@empty<tr><td class="px-3 py-6 text-center text-gray-500">{{ __('No advertisements found.') }}</td></tr>@endforelse</tbody></table><div class="mt-6">{{ $advertisements->links() }}</div></div></div></div></x-app-layout>
